<h2> <%= @congregation.name %> Congregation  #<%= @congregation.number %></h2>
<h3> Address: <%=@congregation.address%> <%=@congregation.street%> </h3>

<br>
<% if current_user.admin %>
Add a new publisher: <%= button_to 'Add a publisher', new_publisher_path, :method => :get %><br>
Add a new territory: <%= button_to 'Add a territory', new_congregation_territory_path(@congregation.id), :method => :get %><br>
Add a new congregation: <%= button_to 'Add a congregation', new_congregation_path, :method => :get %><br>
<% end %>
